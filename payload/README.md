# Payload

This directory is the installable boundary for `killall-skills`.

- `skills/` - reusable skills for global or project install.
- `rules/` - reusable behavioral rules for global or project install.
- `hooks/` - hook authoring instructions and optional hook scripts.

Do not install files from the repository root unless the user explicitly asks.
Root-level files are for maintaining this repository.
